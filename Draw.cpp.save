#include "Engine.h";

Engine::draw()
{
    game_window.clear(sf::Color::white);
    game_window.draw()

        m_Window.draw(m_BackgroundSprite);
    m_Window.draw(m_Bob.getSprite());
    game_window.display();
}
